part of '../../home_screen.dart';

class _Loyalty extends StatelessWidget {
  const _Loyalty();

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: Pad(horizontal: 20),
      child: _LoyaltyCard(),
    );
  }
}
